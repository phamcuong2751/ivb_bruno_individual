info:
  name: Bank User
  type: http
  seq: 5

http:
  method: GET
  url: "{{individual_admin_host}}/bank-user"
  headers:
    - name: platform
      value: WEB
    - name: Authorization
      value: Bearer {{individual_admin_branch_token}}
  body:
    type: json
    data: ""

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
