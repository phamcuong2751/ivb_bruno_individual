info:
  name: Individual List
  type: http
  seq: 1

http:
  method: GET
  url: "{{individual_admin_host}}/sms-templates/4"
  headers:
    - name: platform
      value: WEB
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{individual_admin_branch_token}}"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
