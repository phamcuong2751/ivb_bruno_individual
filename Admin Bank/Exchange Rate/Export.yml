info:
  name: Export
  type: http
  seq: 2

http:
  method: GET
  url: "{{individual_admin_host}}/exchange-rate/export"
  headers:
    - name: platform
      value: WEB
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{individual_admin_branch_token}}"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
