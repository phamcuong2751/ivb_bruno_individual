info:
  name: List Individual Device
  type: http
  seq: 3

http:
  method: GET
  url: "{{individual_admin_host}}/individual-device?pageSize=100&numPage=0&cifCore=1023909"
  headers:
    - name: platform
      value: WEB
    - name: Authorization
      value: Bearer {{individual_admin_token}}
  params:
    - name: pageSize
      value: "100"
      type: query
    - name: numPage
      value: "0"
      type: query
    - name: cifCore
      value: "1023909"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
